test_that("res_fun works", {
  # Place holder, actual tests are below.
  expect_equal(2 * 2, 4)
})

## Test "logit" link
# foo <- function(x, correct = TRUE){
#   if (correct) {
#     tmp <- (pnorm(-abs(x)) * (1 - pnorm(-abs(x))))
#   } else {
#     tmp <- (pnorm(x) * (1 - pnorm(x)))
#   }
#   dnorm(x) / tmp
# }
# plot(x, foo(x), type = "l")
# lines(x, foo(x, correct = FALSE), type = "l", col = "darkred")
